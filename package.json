{
  "name": "DietaApp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "node backend/src/server.js",
    "start:api": "node backend/src/server.js",
    "dev": "npm run clean:ports && vite",
    "dev:nocache": "npm run clean:ports && npm run clean:cache && vite --mode nocache --force",
    "dev:clean": "npm run clean:ports && vite",
    "clean:ports": "powershell -ExecutionPolicy Bypass -File ./clean-ports.ps1",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "preview:nocache": "npm run build && vite preview --mode nocache --port 4173 --strictPort",
    "clean": "rm -rf dist node_modules/.vite",
    "clean:cache": "rm -rf node_modules/.vite",
    "menus:shuffle": "node scripts/shuffleWeeklyMenus.mjs",
    "mobile:android": "npm run build && npx cap sync android && npx cap run android",
    "mobile:android:release": "npm run build && npx cap sync android && cd android && gradlew.bat assembleRelease",
    "mobile:ios:sync": "npm run build && npx cap sync ios",
    "mobile:ios:open": "npx cap open ios",
    "mobile:ios": "npm run mobile:ios:sync && npm run mobile:ios:open"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/app": "^8.1.0",
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "mysql2": "^3.12.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@eslint/js": "^9.39.1",
    "@vitejs/plugin-react": "^5.1.1",
    "eslint": "^9.39.1",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.24",
    "globals": "^16.5.0",
    "vite": "^7.3.1"
  }
}
